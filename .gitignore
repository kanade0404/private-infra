.terraform
credential.json
**/*.tfvars
.terraformrc
tfsec
.bash_history
.config
.terraform.d
actionlint
gcp-credential.json
.aws

# AWS stack (aws/ also has its own .gitignore)
*.tfstate
*.tfstate.*
crash.log
.direnv/
